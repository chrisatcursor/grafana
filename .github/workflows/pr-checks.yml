name: PR Checks
on:
  pull_request_target:
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review
      - labeled
      - unlabeled
      - edited
      - auto_merge_enabled
  issues:
    types:
      - milestoned
      - demilestoned

concurrency:
  group: pr-checks-${{ github.event.number }}

permissions:
  statuses: write
  checks: write
  actions: write
  contents: read
  pull-requests: read

jobs:
  main:
    runs-on: ubuntu-latest
    # pr-checks applies triage labels (e.g. area/backend) that exist on grafana/grafana but are
    # typically not created on personal forks; skip to avoid "label is not defined" failures.
    if: github.event.pull_request.draft == false && github.repository == 'grafana/grafana'
    steps:
      - name: Checkout Actions
        uses: actions/checkout@v5
        with:
          repository: "grafana/grafana-github-actions"
          path: ./actions
          ref: main
          persist-credentials: false
      - name: Install Actions
        run: npm install --production --prefix ./actions
      - name: Run PR Checks
        uses: ./actions/pr-checks
        with:
          token: ${{secrets.GITHUB_TOKEN}}
          configPath: pr-checks
